function fortuneTeller(language, location, buy) {
return `i can speak ${language} fluently and i also live in ${location} eating ${buy}`    
    }
    let ball = fortuneTeller("korea", "south korea","mango")
    console.log(ball);
